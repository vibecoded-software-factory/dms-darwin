name: ci
on:
  push:
    branches: [dev, main]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build
      - name: Selftest
        run: .build/debug/dms-darwin selftest
